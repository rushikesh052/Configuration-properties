spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/eCommerceApp
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
 port: 9090
  config:
    import:
    - optional:configserver:http://localhost:2222/
    
management:
  endpoints:
    web:
      exposure:
        include: '*'
